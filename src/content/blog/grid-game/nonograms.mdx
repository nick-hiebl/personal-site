---
title: 'Nonograms'
description: 'Nonogram rules in my grid game puzzles'
pubDate: '2026-07-15T08:00:00'
---

import { GridPuzzle } from '../../../components/grid-game/GridPuzzle.tsx'
import { basicSchemaGenerator } from '../../../components/grid-game/schema/generators.ts'

A favourite classic puzzle taking place in a grid is nonograms! They're a special type of rule where selected cells in each row and column must be arranged into clusters of certain sizes with at least one empty cell in between.

<GridPuzzle
    isCentered
    schema={basicSchemaGenerator(4, 4, [[1, 2], [1, 1], [1, 1], [2, 1]], [[1, 2], [1, 1], [1, 1], [2, 1]])}
    client:only
/>

<br />

<GridPuzzle
    isCentered
    schema={basicSchemaGenerator(5, 5, [[1, 2], [1, 2], [2, 1], [3], null], [[2], [1, 2], [1, 1], [2, 1], [1, 1]])}
    client:only
/>
